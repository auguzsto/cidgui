import 'package:flutter/material.dart';

abstract class DrawerMenuIcons {
  static const List<Icon> icons = [
    Icon(Icons.person),
    Icon(Icons.help),
  ];
}

abstract class MyMenuIcons {
  static const List<Icon> icons = [
    Icon(Icons.domain_add),
    Icon(Icons.folder_shared),
    Icon(Icons.folder_off),
  ];
}